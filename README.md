# TaskFlow-API
Uma API RESTful moderna para gerenciamento de tarefas (To-Do List), desenvolvida com FastAPI e SQLAlchemy. O projeto utiliza PostgreSQL via Docker para persistência de dados e Alembic para controle de versões e migrações do banco de dados de forma automatizada.
